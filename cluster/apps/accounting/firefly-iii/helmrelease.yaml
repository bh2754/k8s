---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: firefly-iii
  namespace: accounting
spec:
  releaseName: firefly-iii
  targetNamespace: accounting
  interval: 10m
  chart:
    spec:
      chart: firefly-iii
      version: 0.2.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  values:
    installCRDs: false
  install:
    createNamespace: true
