kind: Service
apiVersion: v1
metadata:
  name: celery-results
  namespace: mayan
spec:
  selector:
    app: celery-results
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
