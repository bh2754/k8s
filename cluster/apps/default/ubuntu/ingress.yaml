---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: ubuntu
  namespace: default
spec:
  entryPoints:
    - ssh
  routes:
    - match: HostSNI(`ubuntu.${SECRET_DOMAIN}`)
      services:
        - name: ubuntu
          port: 23
