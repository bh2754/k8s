---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: rdesk
  namespace: default
spec:
  entryPoints:
    - rdp
  routes:
    - match: HostSNI(`*`)
      services:
        - name: rdesk
          port: 3389
