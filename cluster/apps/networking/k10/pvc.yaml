apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: synology-pvc
   namespace: kasten-io
spec:
   storageClassName: nfs-csi
   accessModes:
      - ReadWriteMany
   resources:
      requests:
         storage: 50Gi
