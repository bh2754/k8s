apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: longhorn-pvc
   namespace: kasten-io
spec:
   storageClassName: longhorn
   accessModes:
      - ReadWriteMany
   resources:
      requests:
         storage: 50Gi
