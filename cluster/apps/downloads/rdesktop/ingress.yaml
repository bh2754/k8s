---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: rdesktop
  namespace: downloads
spec:
  entryPoints:
    - rdp
  routes:
    - match: HostSNI(`*`)
      kind: Rule
      services:
        - name: rdesktop
          port: 3389
  tls:
    passthrough: true
