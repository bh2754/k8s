---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: s3
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: HOST(`s3.${SECRET_DOMAIN}`)
      kind: Rule
      services:
        - name: minio
          port: 9000
  tls:
    secretName: ${SECRET_CERT}
