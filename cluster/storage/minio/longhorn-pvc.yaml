---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: &name minio-data-v1
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Mi
