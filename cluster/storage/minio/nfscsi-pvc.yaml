---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-nfscsi
  namespace: minio
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
